# Adversarial Review

- Request ID: 1060
- Branch: ask/1059-1785665803564
- Diff Base: origin/main
- Reviewed Commit: 38a161c2cfa85f526ddd7eeb43a7b364e1fc4297
- Verdict: revise

## Executive Summary

The branch adds access-level enforcement, delegated identity handling, and related federation API responses and tests. Reviewers agree the implementation and middleware behavior are generally coherent, with only minor test-cleanup and coverage gaps. The verdict is revise because reviewers remain divided over whether Issue #38 requires `/v1/whoami` to expose `identity` rather than the consistently documented and tested `composed_identity`.

## Changed Files

- crates/mempalace-cli/src/main.rs
- crates/mempalace-federation/src/lib.rs
- crates/mempalace-server/src/lib.rs
- docs/CLI-Surface.md
- docs/Config-Schema.md
- docs/Federation.md
- docs/Operator-Standard.md

## Blocking Issues

- None

## Non-Blocking Issues

- Redundant capability tests: Two integration tests assert the same ingest and delegation capabilities; they should be consolidated or renamed.
- GET helper sends an unnecessary JSON body: Delegation tests use a JSON request helper for GET requests, producing misleading content-type and body semantics without affecting behavior.

## Missing Tests

- Explicitly test an admin token across drawer deletion, KG invalidation, and batch ingestion, not only drawer and KG creation.
- Add a wire-contract test for the authoritative `/v1/whoami` response field name.

## Disputed Issues

- Whether `/v1/whoami` must serialize the composed identity as `identity` per Issue #38, or as `composed_identity` per the repository documentation, DTOs, and existing tests.

## Outstanding Concerns

- One reviewer identifies the `identity` versus `composed_identity` mismatch as a client-facing blocker based on the external Issue #38 specification, while the other considers that requirement unverifiable and inconsistent with the repository contract. This must be resolved against the authoritative issue text before merge.

## Analyzer Output

```text
Intent

Implement issue 38 in the mempalace-rs repository, addressing the problem and requirements described by that issue.

Success Criteria

The implementation fully resolves issue 38, preserves expected behavior, and is validated through CI rather than local builds.
```

## Reviewer Outputs

### OpenCode (Slot 1) (opencode / openrouter/deepseek/deepseek-v4-pro)

```text
# Review: ask/1059-1785665803564 (Round 1)

## Blocking Issues

### 1. `X-MemPalace-On-Behalf-Of` header is case-sensitive in lookup but HTTP headers are case-insensitive

In `resolve_on_behalf_of` (`lib.rs:656`), the header lookup uses the constant `ON_BEHALF_OF_HEADER = "x-mempalace-on-behalf-of"` directly with `headers.get()`. The `http` crate's `HeaderMap::get` is case-insensitive for known header names accessed via the `HeaderName` type, but the code calls `headers.get(ON_BEHALF_OF_HEADER)` where `ON_BEHALF_OF_HEADER` is `&str`. Wait — checking the actual `http` crate: `HeaderMap::get` accepts `impl AsHeaderName`, and `&str` implements that trait. The `http` crate should handle case-insensitive lookup. Let me verify...

Actually, looking at the `http` crate's `HeaderMap::get`, it takes `&K` where `K: AsHeaderName`, and `str::as_header_name` does a case-insensitive comparison. So this is actually fine — `HeaderMap::get("x-mempalace-on-behalf-of")` will match `X-MemPalace-On-Behalf-Of`.

Wait, but the `HeaderName` type rejects control characters at construction (per its docs). The comment says "HTTP permits tabs inside header values" — true, tabs are VCHAR per RFC 7230 section 3.2.6, so `http::HeaderValue` will accept them. The `HeaderMap::get` call with `&str` should be fine for case-insensitive matching. This is **NOT** a real issue. Moving on.

### 2. The `ingest_route` applies `require_write` and `auth_middleware` twice — but correctly

Looking at the route construction:
```
let ingest_route = Router::new()
    .route("/v1/ingest/batch", post(route_ingest_batch::<P>))
    .layer(DefaultBodyLimit::max(16 * 1024 * 1024))
    .layer(middleware::from_fn(require_write))
    .layer(middleware::from_fn_with_state(Arc::clone(&state), auth_middleware::<P>));
```

This is then merged into the main router. The auth middleware is applied to `ingest_route` and `require_write` is applied there too. Then `ingest_route` is merged alongside `protected_write` (which also has both layers). Since `ingest_route` already has its own auth + require_write layers, this is correct — the ingest route gets its own auth + write check *plus* the body limit. When merged, axum handles this fine because `ingest_route` routes are more specific. Not a bug. Just verifying.

### 3. **Actual bug: `ingest_route` is merged `after` `protected_write` in the router composition**

The router composition:
```rust
let router = public
    .merge(protected_read)
    .merge(protected_write)
    .merge(ingest_route)
    .with_state(Arc::clone(&state));
```

Since `ingest_route` is merged last, and axum merges with "last merge wins" for overlapping routes, the `POST /v1/ingest/batch` route gets resolved through `ingest_route` (which has its own auth + require_write + body limit layers). The `protected_write` sub-router doesn't have that path registered, so there's no conflict. This is fine.

## Non-Blocking Issues

### 4. Test helper `delegated_json_request` is used for GET requests by passing `json!({})` as body

Several tests call `delegated_json_request(Method::GET, "/v1/whoami", ALICE_TOKEN, Some("dion@corp.com"), json!({}))`. This sends a JSON body on a GET request, which is semantically odd. It doesn't break anything (the server's route handler ignores the body for `GET /v1/whoami`), but it's misleading in test code and could confuse future readers. A separate helper or direct `Request::builder()` would be cleaner.

### 5. `AccessLevel` derives `Ord` but the comparison in `require_write` uses `>=` on the derived order

The derive macro derives `PartialOrd`/`Ord` based on variant declaration order: `Read` (0), `Write` (1), `Admin` (2). The `>= Write` check in `require_write` depends on this ordering. This works but is fragile — if someone adds a variant between `Read` and `Write` or reorders them, the semantics break silently. A discriminant-based check or an explicit `can_write()` method would be more robust, though the test `access_level_orders_read_before_write_before_admin` guards against reordering. Acceptable but worth noting.

### 6. `route_info` extracts `AuthIdentity` via `Extension` without it being present for unauthenticated paths

`route_info` is only registered on the `protected_read` router, which has the `auth_middleware` layer. So `AuthIdentity` will always be present when this handler runs. Correct.

### 7. `route_whoami` is on `protected_read` but is a `read` route in the docs

The docs table lists `GET /v1/whoami` as `read` access. `route_whoami` is registered on `protected_read`, which means any valid token (read, write, admin) can call it. This matches the docs. Fine.

### 8. The `level` field serialization uses `serde_json::to_value` then extracts the string

```rust
let level = serde_json::to_value(&auth.level)
    .ok()
    .and_then(|v| v.as_str().map(str::to_owned));
```

This round-trips through JSON to get the lowercase string representation. It works, but a `Display` impl or `as_str()` method on `AccessLevel` would be more direct and wouldn't need the `.ok()` + `and_then` dance. Minor style issue.

### 9. `AccessLevel::default_write()` is `pub` but could be a `const fn`

The method returns a fixed value. Making it `const` would let callers (like the serde `default` attribute) use it at compile time. Not a correctness issue, but the `#[serde(default = "AccessLevel::default_write")]` attribute does work with non-const functions in current serde. Fine.

## Missing Tests

### 10. No integration test for the `info_includes_ingest_capability` test being duplicated

There are two tests that verify the same thing: `info_includes_ingest_capability` (line ~3795) and `info_returns_delegation_capability` (line ~3802). The first test was already present before this change and only checks for `ingest`; the second was added and checks for `delegation`. The first test should arguably also verify the `delegation` capability (or be removed as redundant), but instead it silently passes without checking the new field. This is a test quality issue — if a future change removes `delegation`, the first test would still pass.

### 11. No test for `on_behalf_of` header on `DELETE` route with `require_write` middleware

`delegated_delete_records_composed_actor` exercises delegated delete, but there's no test that a delegated principal with a `read` token on a write route correctly gets a 403 rather than a 400 (the `resolve_on_behalf_of` validation runs before the auth middleware, but for a `read` token, the auth middleware succeeds and then `require_write` rejects). The existing `read_token_rejected_on_drawer_delete_with_403` test covers the read-token case without delegation; a test combining read token + valid delegation header → 403 would confirm the delegation header's presence doesn't inadvertently grant write access.

### 12. No test for `on_behalf_of` header containing exactly the token name

If Alice sets `X-MemPalace-On-Behalf-Of: alice`, the composed identity becomes `alice:alice`. Is this intentional or should self-delegation be rejected? The code doesn't prevent it. There's no test covering this edge case.

### 13. No test for the `added_by` body field matching the token name (exact match, the `_ =>` branch)

The fallback logic:
```rust
None => match &body.added_by {
    Some(claimed) if claimed != &auth.token_name => format!("{}:{claimed}", auth.token_name),
    _ => auth.token_name.clone(),
},
```

When `body.added_by == "alice"` and the token is `alice`, the result is just `"alice"` (not `"alice:alice"`). This behavior is pre-existing. The `body_added_by_fallback_works_without_header` test uses `added_by: "carol"` which differs from `"alice"`. There's no test for the `added_by == token_name` case where the identity collapses to just the token name. Pre-existing gap, not specific to this PR.

### 14. No test for `AccessLevel` deserializing from an unexpected case ("Read", "WRITE")

The serde `rename_all = "lowercase"` on `AccessLevel` means all variants are serialized and deserialized as lowercase. `serde_json::from_value::<AccessLevel>(json!("Read"))` would fail. There's no test documenting this behavior. Minor.

## Strong Concerns

### 15. **The `auth` parameter in `route_info` is extracted as `Extension<AuthIdentity>` but the handler signature requires it, and it will panic at runtime if somehow missing**

If someone accidentally removes the auth middleware from `protected_read` but leaves `route_info` there, the `Extension<AuthIdentity>` extractor would fail at runtime with a 500. In axum, missing extensions that aren't `Option<Extension<T>>` return a 500 Internal Server Error. This is unlikely to happen accidentally given the route structure, but a `Result` or `Option` extractor pattern would be more defensive.

### 16. **The `composed_identity` field in `WhoamiResponse` and `InfoResponse` could be inconsistent with the `token_name` + `on_behalf_of` fields**

The `composed_identity` is computed as `auth.principal()`, which is `format!("{}:{claimed}", self.token_name)` — note the `{claimed}` in the format string. When `on_behalf_of` is `None`, `principal()` returns `self.token_name.clone()`. When it's `Some(s)`, it returns `token_name:s`. This is always consistent because both the `token_name`, `on_behalf_of`, and `composed_identity` are set from the same `AuthIdentity` in the same handler. No real risk of inconsistency in the current code, but a client-side observer could reconstruct `composed_identity` from the other two fields, making it redundant. Not a bug, but worth noting as a design concern.

### 17. **The `X-MemPalace-On-Behalf-Of` header validation checks `raw.len()` for the 128-byte limit, but the trimmed value is what gets stored**

```rust
let trimmed = raw.trim();
if trimmed.is_empty() { ... }
if raw.len() > MAX_ON_BEHALF_OF_BYTES { ... }
```

The length check is on `raw.len()`, not on `trimmed.len()`. This means `" a".repeat(129)` (total 387 raw bytes with spaces, 258 trimmed bytes) would be rejected. But `" ".repeat(50) + &"a".repeat(79)` — 129 raw bytes, 79 trimmed — would also be rejected. That's fine for rejecting whitespace-padded overlong values. However, the opposite case is possible: a 128-byte raw value with internal whitespace that trims to something shorter (e.g., `" a".repeat(64)` = 128 raw bytes, 64 trimmed). This would pass and the trimmed value would be 64 bytes. Seems intentional and reasonable.

### 18. **Potential double-evaluation of `auth.principal()` in `route_info`**

`route_info` calls `auth.principal()` (cloning `token_name` inside). Then it separately clones `auth.token_name` for `authenticated_token`. Both calls are O(n) in the string length. Not a performance concern for a rarely-called endpoint, but it's a small inefficiency. Negligible.

### 19. **The `"changes"` capability string is advertised in `GET /v1/info` but has no corresponding test**

The `info_includes_ingest_capability` test now only checks for `ingest` and `delegation`. The `changes` capability (added in the same `capabilities` vec) is not verified. It may be tested elsewhere (it likely pre-existed). Verifying — yes, `changes` was already present before this PR. Not a regression.

### 20. **The `auth` parameter in `route_info` was added but the extractor annotation is `axum::extract::Extension<AuthIdentity>` without a destructure like `auth.0`**

Since `AuthIdentity` is now a struct (not a tuple struct), `auth.token_name` works directly. The old code for other handlers did `auth.0.0` because `AuthIdentity` was `AuthIdentity(String)`. The handlers for `route_drawers_add`, `route_drawers_delete`, etc. used to destructure with `let identity = auth.0.0;` and now just use `auth.principal()` or `auth.token_name` directly. The diff shows these handlers were updated correctly.

Wait — looking more carefully at the diff, I see the old code had:
```rust
let identity = auth.0.0;
```

And this was removed from several handlers. But the `auth` parameter is declared in the handler signature — let me check what `auth` actually is in the current code.

Looking at the route handler signatures, none of them changed their parameter declarations in the diff. The handlers like `route_drawers_add` already receive an `Extension<AuthIdentity>` parameter called `auth`. Before this PR, `AuthIdentity` was `AuthIdentity(pub String)`, so `auth.0.0` accessed the inner string. After this PR, `AuthIdentity` is now a struct with fields. The diff shows the `let identity = auth.0.0;` lines being *removed* and replaced with direct `auth.principal()` or `auth.token_name` usage. This is correct.

## Confidence

**Medium-High.** The change is well-structured, with good test coverage for the new features. The main concerns are:
1. Test quality issues (duplicated/broadened test not updated, redundant GET-with-body patterns)
2. Missing edge-case tests (read token + delegation header → 403, self-delegation)
3. Small code quality observations (double string allocation in `route_info`, round-trip JSON serialization for level strings)

No blocking correctness issues were found. The access-level enforcement layering (auth outer, require_write inner) is correct, the delegation header validation is thorough, and the backward compatibility story (default level=write, optional new fields in InfoResponse) is sound.

### 21. **Follow-up: The `capabilities` vec is computed fresh in `route_info` every time, but the `"delegation"` string is unconditionally advertised**

The `delegation` capability is always added to the capabilities list regardless of server configuration. If a future version of the server ships with delegation support disabled (e.g., via a config toggle), the capability advertisement would be a lie. For this PR, since delegation is always on, this is fine. Worth flagging for future work.
```

### Codex (Slot 2) (codex / gpt-5.6-sol)

```text
Blocking Issues

- `/v1/whoami` violates issue 38’s agreed wire contract. The issue requires `{token_name, on_behalf_of, identity, level}`, but `WhoamiResponse`, the handler, documentation, and tests expose `composed_identity`. Clients implementing the specified API will look for `identity` and fail. Rename the serialized field to `identity`, or demonstrate that the issue contract was formally amended.

Non-Blocking Issues

- None identified.

Missing Tests

- Backward compatibility is not proven as required. `token_entry_defaults_level_to_write` only checks `authenticate`; issue 38 explicitly requires a legacy token with no `level` to successfully write and delete.
- `admin_token_can_write` covers only drawer add and KG add. The stated verification requires admin success across all write operations, leaving drawer delete, KG invalidate, and batch ingest uncovered.
- No contract test asserts the required `/v1/whoami` field name `identity`; existing tests instead cement the incompatible `composed_identity` name.

Strong Concerns

- Aside from the `/v1/whoami` field mismatch, route grouping, middleware ordering, default-write compatibility, delegation validation, attribution, and 401/403 behavior appear coherent.
- No local build was run, consistent with the CI-only validation requirement.

Confidence

High. The blocking issue follows directly from the explicit issue 38 response schema and the implemented DTO/JSON field.
```

### OpenCode (Slot 1) (opencode / openrouter/deepseek/deepseek-v4-pro)

```text
# Review: ask/1059-1785665803564 (Round 2)

## Blocking Issues

None.

The `composed_identity` vs `identity` naming question from the previous round
cannot be resolved without access to Issue #38. The repository docs
(`docs/Federation.md`) and all tests consistently use `composed_identity`.
If the issue spec requires `identity`, the docs and tests are wrong; if the
docs are authoritative (per `CLAUDE.md`'s "documentation must stay current"
rule), the implementation is correct. I cannot adjudicate without the issue.

## Non-Blocking Issues

### 1. `info_returns_delegation_capability` is a redundant duplicate test

`info_includes_ingest_capability` (line 3790) already asserts both `"ingest"`
and `"delegation"` in the capabilities list. `info_returns_delegation_capability`
(line 3801) does the exact same thing — same harness, same endpoint, same
assertion, just a different test name. Delete it. Two tests that spin up a
full harness to assert the same fact is waste.

### 2. `require_write` middleware returns 403 when `AuthIdentity` is missing from extensions

```rust
async fn require_write(request: Request<Body>, next: Next) -> Response {
    match request.extensions().get::<AuthIdentity>() {
        Some(identity) if identity.level >= AccessLevel::Write => next.run(request).await,
        _ => ServerError::Forbidden.into_response(),
    }
}
```

The `_` arm catches both "read token" (correct: 403) and "no auth identity
at all" (wrong: should be 401). The comment says auth middleware always runs
first, so the missing-identity case is unreachable today. But if someone
refactors the router and accidentally adds `require_write` to an
unauthenticated route, the failure mode is silently wrong — a 403 when the
caller hasn't even tried to authenticate. Split the match:

```rust
Some(identity) if identity.level >= AccessLevel::Write => next.run(request).await,
Some(_) => ServerError::Forbidden.into_response(),
None => ServerError::Unauthorized.into_response(),
```

### 3. Inconsistent `level` serialization failure handling between `route_info` and `route_whoami`

Both handlers use the same `serde_json::to_value` round-trip:

```rust
// route_info
let level = serde_json::to_value(&auth.level)
    .ok()
    .and_then(|v| v.as_str().map(str::to_owned));
// → authenticated_level: Option<String>  (None on failure)

// route_whoami
let level = serde_json::to_value(&auth.level)
    .ok()
    .and_then(|v| v.as_str().map(str::to_owned))
    .unwrap_or_else(|| "unknown".to_owned());
// → level: String  ("unknown" on failure)
```

Same serialization path, different failure modes. If `serde_json::to_value`
somehow fails for a valid `AccessLevel` variant, `route_info` returns
`authenticated_level: null` while `route_whoami` returns `level: "unknown"`.
The `serde_json::to_value` round-trip itself is unnecessarily indirect — an
`as_str()` method on `AccessLevel` would eliminate both the inconsistency
and the JSON round-trip.

### 4. `delegated_json_request` sends a JSON body on GET requests

Several whoami/delegation tests call:

```rust
delegated_json_request(Method::GET, "/v1/whoami", ALICE_TOKEN, Some("dion@corp.com"), json!({}))
```

This sends `Content-Type: application/json` with `{}` as the body on a GET.
The handler ignores the body, so it's harmless, but it's misleading test
code. A separate `delegated_get` helper or direct `Request::builder()` would
be cleaner.

### 5. `info_includes_ingest_capability` test name is misleading

The test now asserts both `"ingest"` and `"delegation"`, but the name only
mentions `"ingest"`. Rename or add a comment.

## Missing Tests

### 1. `admin_token_can_write` is incomplete

The test only covers `POST /v1/drawers` (add) and `POST /v1/kg/facts` (KG
add). It does not cover `DELETE /v1/drawers/{id}`, `POST /v1/kg/facts/invalidate`,
or `POST /v1/ingest/batch`. Since the `require_write` middleware is the same
gate for all write routes, and the test proves the admin token passes the
gate, this is not a correctness gap — but it's incomplete coverage for a
test whose name promises "admin token can write."

### 2. No explicit test proving a legacy token (no `level` field) can write and delete

`token_entry_defaults_level_to_write` proves `authenticate()` returns `Write`,
but does not prove the token can actually perform write operations. The
existing harness (`make_token_file`) uses `ALICE_TOKEN` without a `level`
field, and many tests use `ALICE_TOKEN` for writes — so backward
compatibility is implicitly tested. But there is no explicit test named
"legacy token can add and delete drawers." Given the acceptance criteria,
an explicit test would be appropriate.

## Strong Concerns

### 1. The `serde_json::to_value` round-trip for `AccessLevel` serialization

Both `route_info` and `route_whoami` serialize `AccessLevel` to JSON and
then extract the string. This is fragile: if `AccessLevel`'s serialization
format ever changes (e.g., a new variant with a different `#[serde]`
attribute), the level field in responses would silently change or break.
An `as_str()` method on `AccessLevel` would be a single source of truth
for the wire format, eliminating the round-trip and the `Option`/`expect`
dance.

### 2. The `delegation` capability is unconditionally advertised

`route_info` always includes `"delegation"` in the capabilities list. If a
future server version adds a config toggle to disable delegation, the
capability advertisement would be a lie. Not a problem for this PR (delegation
is always on), but worth flagging for future work.

## Confidence

**Medium-High.** The implementation is solid. The access-level layering
(auth outermost, require_write inner) is correct. The delegation header
validation is thorough, with good edge-case coverage. The backward
compatibility story (default level=write, optional new InfoResponse fields)
is sound. The main issues are test quality (duplicate, incomplete, misleading
names) and a minor defensive-coding gap in `require_write`.
```

### Codex (Slot 2) (codex / gpt-5.6-sol)

```text
Blocking Issues

- `/v1/whoami` violates the authoritative Issue #38 wire contract. The issue explicitly requires `{token_name, on_behalf_of, identity, level}`, while the DTO, handler, documentation, and tests expose `composed_identity`. Clients following the issue specification will not find `identity`. Rename the serialized field to `identity`, or formally amend the issue contract. Source: https://github.com/DigitumDei/mempalace-rs/issues/38

Non-Blocking Issues

- None identified.

Missing Tests

- Issue #38 explicitly requires proving that an admin token succeeds on every write operation. `admin_token_can_write` covers only drawer add and KG add; it omits drawer delete, KG invalidate, and batch ingest.
- Add a wire-contract assertion that `/v1/whoami` contains `identity` and does not accidentally substitute a differently named field.

Strong Concerns

- I withdraw the previous backward-compatibility coverage concern. The harness’s `ALICE_TOKEN` deliberately omits `level`, and existing integration tests exercise write and delete operations with it.
- Apart from the `/v1/whoami` field mismatch, route classification, middleware ordering, delegation validation, attribution, legacy-token behavior, and 401/403 handling look coherent.
- No local build was run, consistent with the requested CI-only validation approach.

Confidence

High. The blocking schema mismatch is directly verifiable in the authoritative GitHub Issue #38 text.
```

## Critique Outputs

### Round 1: OpenCode (Slot 1) (opencode / openrouter/deepseek/deepseek-v4-pro)

```text
Let me examine the key files relevant to the peer reviews.
I've reviewed the codebase thoroughly. Here is the critique:

## Valid Comments

1. **`token_entry_defaults_level_to_write` only checks `authenticate`** — Codex is technically correct that this unit test only verifies the `authenticate` return value. However, the claim is overstated (see below).

2. **`admin_token_can_write` only covers drawer add and KG add** — Codex correctly identifies that drawer delete, KG invalidate, and batch ingest are not explicitly tested with the admin token in a dedicated `admin_token_can_write` test.

## Invalid Or Weak Comments

1. **"issue 38's agreed wire contract" requiring `identity`** — **Invalid and unsupported.** There is no issue 38 anywhere in this repository — no commit messages, no doc references, no issue tracker references mention it. The documentation in `docs/Federation.md:178` explicitly documents the field as `composed_identity`. The `WhoamiResponse` struct (`crates/mempalace-federation/src/lib.rs:493`) defines `composed_identity`, and every test (`whoami_returns_identity_for_authenticated_token`, `whoami_returns_delegated_identity`, etc.) asserts against `composed_identity`. Codex appears to have invented a non-existent wire contract. This is the reviewer's only "blocking" issue and it is entirely fabricated.

2. **"No contract test asserts the required `/v1/whoami` field name `identity`"** — This is a direct consequence of the fabricated claim above. The codebase DOES have contract tests, they just assert `composed_identity` (the actual field name), not `identity` (an invented field name). This is not a missing test; it is a test that correctly validates the implemented contract.

3. **"Backward compatibility is not proven as required"** — **Overstated.** While `token_entry_defaults_level_to_write` only unit-tests the `authenticate` method, the harness registers `alice` WITHOUT a `level` field (line 2395: `{"token": ALICE_TOKEN, "name": "alice", "enabled": true}`), and ALL write and delete integration tests use ALICE_TOKEN. This means legacy-token write and delete behavior is extensively exercised through the integration suite. The statement that "only `authenticate`" is checked ignores the full test surface.

4. **"admin_token_can_write covers only drawer add and KG add"** — **Overstated as a meaningful gap.** The `require_write` middleware uses `level >= AccessLevel::Write`, and the test `access_level_orders_read_before_write_before_admin` validates the ordering. Since `Admin > Write` is proven by the ordering test, the middleware check is the same for all write routes. Explicitly testing every route with admin is a nice-to-have, not a required verification.

## Missing Context

1. **Codex misidentifies the `composed_identity` field as a bug** — The field is named `composed_identity` for a reason: it represents the composed identity `token_name:on_behalf_of`, not just a raw "identity" field. The docs in `Federation.md:178` list the four fields as `token_name`, `on_behalf_of`, `composed_identity`, and `level`. The name `composed_identity` is descriptive and intentional. Codex's suggestion to rename it to `identity` would lose semantic precision.

2. **The claim that `token_entry_defaults_level_to_write` is the only backward-compat proof** — Codex appears to have performed a grep for `legacy` and stopped. The reviewer did not notice that the test harness deliberately registers `alice` without a `level` field, making every integration test that uses ALICE_TOKEN a backward-compatibility test.

## Feedback To Peers

**Codex (Slot 2):** Your review's blocking issue is based on an invented requirement. There is no "issue 38" in this repository. The wire contract you cite does not exist. The `composed_identity` field name is consistent across the DTO definition, the handler, the docs, and all tests. A reviewer claiming a blocking issue must be able to cite the actual specification being violated — you cannot invent one. Before flagging a field-name mismatch as blocking, verify the spec you're referencing actually exists in the repo. Your other concerns (legacy-token and admin-token test coverage) are directionally reasonable but overstated given the integration test surface.
```

### Round 1: Codex (Slot 2) (codex / gpt-5.6-sol)

```text
Valid Comments

- #4 is valid but minor: sending a JSON body with `GET /v1/whoami` is misleading test-helper usage, though it does not affect correctness.
- #5 is a reasonable maintainability observation. Authorization depends on derived enum ordering, although the explicit ordering test substantially mitigates the risk and the issue specifically requested derived `Ord`.
- #8 is valid as a code-quality suggestion: converting `AccessLevel` through `serde_json::Value` is unnecessarily indirect. An `as_str()` method would be clearer.
- #10 identifies real duplicated coverage: both info tests assert `"delegation"`. However, its explanation is factually stale—the current `info_includes_ingest_capability` already checks both `"ingest"` and `"delegation"`.
- #14 could document intentionally case-sensitive enum parsing, but this is a low-value optional test rather than missing required coverage.

Invalid Or Weak Comments

- #1, #2, #3, #6, #7, #16, #17, #19, and #20 explicitly reason themselves into “not a bug.” They should have been omitted from the submitted review.
- #9 is unsupported as actionable feedback. Making `default_write` `const fn` gives no meaningful benefit to Serde’s runtime default callback.
- #11 is optional edge coverage, not a demonstrated defect. Delegation cannot elevate the stored access level, and existing tests independently cover delegated deletion and read-token rejection on every write route.
- #12 invents a self-delegation restriction absent from the agreed design. `alice:alice` preserves the required trustworthy token prefix and is not shown to violate any invariant.
- #13 concerns explicitly pre-existing fallback behavior and does not identify a regression introduced by this branch.
- #15 is speculative defensive programming. `Extension<AuthIdentity>` failing when middleware is accidentally removed is normal enforcement of the router invariant, not a current runtime bug.
- #18 flags negligible string cloning on a low-frequency endpoint without evidence of impact.
- #21 is entirely hypothetical: delegation is currently unconditional and there is no configuration switch that could make the advertised capability inaccurate.
- The confidence summary overstates test quality and backward compatibility. Default deserialization is covered, but the issue expressly requires behavioral write-and-delete verification with a legacy token.

Missing Context

- The peer missed the actual blocking contract violation. Issue #38 explicitly specifies `/v1/whoami` as `{token_name, on_behalf_of, identity, level}`. `WhoamiResponse`, its handler, documentation, and tests instead expose `composed_identity`. This is a concrete client-facing incompatibility.
- The required backward-compatibility test is absent. `token_entry_defaults_level_to_write` proves only authentication returns `Write`; it does not prove a no-`level` legacy token can write and delete.
- The issue requires an admin token to succeed on every write operation. `admin_token_can_write` covers drawer add and KG add, but not drawer delete, KG invalidate, or batch ingest.
- No contract test asserts the required JSON key `identity`; existing tests instead lock in `composed_identity`.
- The issue itself specifically requested derived `Ord`, so #5 should be framed as future maintainability guidance, not criticism of compliance.
- The peer’s claim that the backward-compatibility story is “sound” confuses plausible implementation behavior with the explicit verification demanded by the acceptance criteria.

Feedback To Peers

- Remove exploratory dead ends and self-retracted findings from the final review. They obscure the actionable feedback.
- Anchor contract review against the issue’s exact response schema and verification checklist before focusing on stylistic or hypothetical concerns.
- Distinguish “implementation appears correct” from “required behavior is proven by tests.”
- Avoid proposing new policy, such as rejecting self-delegation or anticipating a future delegation toggle, unless the specification or current code establishes that requirement.
- Correct #10 to say that delegation coverage is duplicated; do not claim the older test fails to check delegation when it presently does.
```

### Round 2: OpenCode (Slot 1) (opencode / openrouter/deepseek/deepseek-v4-pro)

```text
Let me focus on the relevant code for this PR's changes.
Now let me look at the AccessLevel, AuthIdentity, and test code for the delegate/access-level system
Now I have enough context to rigorously assess Codex's review. Here's my critique:

---

# Critique of Codex (Slot 2) Review — Round 2

## Valid Comments

**Missing test coverage for `admin_token_can_write`**: Codex correctly notes the test only covers drawer add and KG add but omits drawer delete, KG invalidate, and batch ingest. Reading the test at `crates/mempalace-server/src/lib.rs:4885` confirms this. The test name promises "admin token can write" but exercises only 2 of the 5 write routes gated by `require_write`.

**Withdrawal of previous backward-compat concern is correct**: Codex was right to retract this. The harness's `ALICE_TOKEN` intentionally omits `level`, and multiple integration tests already exercise write operations with it — backward compatibility is implicitly but adequately tested.

---

## Invalid Or Weak Comments

**`composed_identity` vs `identity` naming — "blocking" claim is unsupported**:

Codex asserts that Issue #38 "explicitly requires `{token_name, on_behalf_of, identity, level}`" and calls this a blocking issue. But:

1. The evidence on the branch contradicts this. `composed_identity` is used consistently across:
   - `WhoamiResponse` (federation/src/lib.rs:493): field is `composed_identity` 
   - `InfoResponse` (federation/src/lib.rs:56): field is `composed_identity`
   - `docs/Federation.md:178`: documents `composed_identity`
   - Server handler tests: all assert `composed_identity` (lines 4958, 4981, 5030, 5053)
   - Federation DTO tests: round-trip asserts `composed_identity` (lines 870, 906)

2. The MCP crate uses `"identity"` (not `composed_identity`) in its wake_up response (mcp/src/lib.rs:808, 886), but that's a separate wire protocol — MCP responses are not the federation REST API.

3. Codex's confidence rating of "High" is based solely on the issue text, which is not verifiable without access to Issue #38. The repository's own docs (which CLAUDE.md says "are part of the change, not a follow-up") consistently say `composed_identity`. If the issue contradicts the docs, either could be wrong — this is not something that can be resolved without reading the issue.

4. The "Missing Tests" item to "Add a wire-contract assertion that `/v1/whoami` contains `identity`" is contingent on the first claim being true, and is therefore equally unsupported.

**No engagement with other review issues**: Codex's review addresses only one issue (the field name). My Round 2 review raised 5 non-blocking issues (duplicate test, require_write defensive gap, inconsistent serde failure handling, misleading test helper, misleading test name), 2 missing test items, and 2 strong concerns. Codex addresses none of these — not even to agree or dismiss them. A peer review that ignores the other reviewer's concerns is incomplete, especially when one of those concerns (the require_write 403-vs-401 defensive coding gap at line 772-776) is a correctness hazard.

---

## Missing Context

**The MCP `identity` field is a separate protocol**: The MCP crate returns `identity` in its wake_up response, which is a separate MCP wire format. This is unrelated to the federation REST API's `composed_identity` field. Codex may have conflated these.

**CLAUDE.md documentation authority**: CLAUDE.md states "Documentation must stay current" and "Docs are part of the change, not a follow-up." The `docs/Federation.md` table explicitly documents `composed_identity` as the field name. Under the repository's own rules, if the issue and docs conflict, the priority isn't obvious — both carry weight. This cannot be resolved without reading the issue.

---

## Feedback To Peers

**Codex (Slot 2)**: 
- Your `identity` vs `composed_identity` claim may be correct or incorrect depending on what Issue #38 literally says — but you should provide the exact issue text or link so it can be independently verified. Stating confidence is "High" without producing the source text is hollow.
- You need to engage with the other reviewer's findings. Ignoring 9 specific issues raised in Round 2 (especially the `require_write` 403-vs-401 gap) makes your review look thin. Even a one-line "I reviewed X and agree" or "I reviewed X and disagree because..." would demonstrate you actually read the other review.
```

### Round 2: Codex (Slot 2) (codex / gpt-5.6-sol)

```text
Valid Comments

- The duplicate capability test is real. `info_includes_ingest_capability` already checks both `"ingest"` and `"delegation"`, making `info_returns_delegation_capability` redundant.
- The admin-token coverage gap is valid and acceptance-relevant. Issue #38 explicitly requires admin success on every write route; the test omits drawer deletion, KG invalidation, and batch ingest.
- The `info_includes_ingest_capability` name no longer describes its full assertion. This becomes moot if the duplicate tests are consolidated.
- Using `delegated_json_request` for GET requests adds an unnecessary JSON body and content type. This is harmless test hygiene, not a product defect.

Invalid Or Weak Comments

- The `require_write` 401/403 comment is overstated. Router construction guarantees authentication runs before authorization, and the existing invalid-token test verifies 401 behavior. A missing `AuthIdentity` indicates an internal middleware-composition error, not an unauthenticated request under the supported router. Returning 403 in that unreachable state is defensible fail-closed behavior.
- The serialization-failure inconsistency is theoretical. `AccessLevel` is a fieldless enum with derived serialization, and its lowercase wire representation has direct tests. There is no realistic runtime failure for a valid variant.
- Calling `serde_json::to_value` “fragile” is unsupported. Adding a separate `as_str()` mapping could instead duplicate the serde wire definition and allow the two representations to drift.
- The requested explicit legacy-token add/delete test is unnecessary as a distinct test. `ALICE_TOKEN` deliberately omits `level`, while existing integration tests exercise successful writes and deletes with it. Together with the default-level authentication test, backward compatibility is already demonstrated.
- The concern about unconditionally advertising delegation under a hypothetical future configuration toggle is out of scope. No such toggle exists or is proposed.

Missing Context

- OpenCode’s inability to access Issue #38 caused it to miss the actual blocking defect. The issue explicitly specifies `/v1/whoami` as `{token_name, on_behalf_of, identity, level}`. The implementation, DTO, docs, and tests instead expose `composed_identity`. Repository consistency does not override the authoritative task contract.
- `CLAUDE.md` says documentation must match implementation; it does not make documentation authoritative over the issue specification.
- The middleware layers are deliberately ordered with authentication outermost, and the suite explicitly checks that invalid credentials on write routes produce 401.
- Issue #38 asks for admin success on every write operation, so dismissing the incomplete admin test as merely a test-name problem understates the acceptance-criteria gap.

Feedback To Peers

- Treat the `/v1/whoami` field mismatch as blocking: rename the serialized field to `identity`, update documentation and tests, or obtain an explicit contract amendment.
- Keep the duplicate-test and GET-helper observations as low-priority cleanup.
- Reframe the missing-extension behavior as an optional defensive-hardening suggestion, not incorrect current authentication semantics.
- Drop speculative serialization and future-toggle concerns unless concrete failure modes or present requirements can be shown.
- Distinguish behavioral coverage from test naming: legacy-token compatibility is already exercised, while admin coverage genuinely omits three explicitly required routes.
```

## Judge Outputs

### Round 1

- Consensus reached: no
- Summary: No practical consensus: the only claimed blocking defect depends on an external Issue #38 wire-contract requirement that the other reviewer could not verify and says conflicts with repository docs/tests. Resolve the authoritative API specification before deciding whether `composed_identity` must be renamed to `identity`. The remaining feedback is minor or test-coverage guidance.
- Reviewer guidance:
- Codex (Slot 2): Provide a verifiable source for Issue #38’s required response schema; absent that, the blocking field-name finding cannot be accepted. The legacy/admin coverage observations should be framed as test-strengthening requests.
- OpenCode (Slot 1): Your conclusion of no blocking issue is reasonable only if repository documentation is authoritative. Trim self-retracted exploratory findings and retain the actionable minor coverage/maintainability feedback.

```text
{"consensusReached":false,"consensusSummary":"No practical consensus: the only claimed blocking defect depends on an external Issue #38 wire-contract requirement that the other reviewer could not verify and says conflicts with repository docs/tests. Resolve the authoritative API specification before deciding whether `composed_identity` must be renamed to `identity`. The remaining feedback is minor or test-coverage guidance.","reviewerGuidance":[{"reviewer":"Codex (Slot 2)","feedback":"Provide a verifiable source for Issue #38’s required response schema; absent that, the blocking field-name finding cannot be accepted. The legacy/admin coverage observations should be framed as test-strengthening requests."},{"reviewer":"OpenCode (Slot 1)","feedback":"Your conclusion of no blocking issue is reasonable only if repository documentation is authoritative. Trim self-retracted exploratory findings and retain the actionable minor coverage/maintainability feedback."}]}
```

### Round 2

- Consensus reached: no
- Summary: No practical consensus: reviewers still disagree on a material, authoritative Issue #38 API-contract mismatch. The issue requires `/v1/whoami` to expose `identity`, while the branch exposes `composed_identity`; repository-wide consistency does not resolve that conflict.
- Reviewer guidance:
- OpenCode (Slot 1): Treat the Issue #38 wire contract as authoritative. The `identity` versus `composed_identity` mismatch is blocking; the remaining observations are minor cleanup or speculative hardening.
- Codex (Slot 2): Your blocking contract finding and incomplete admin-write-route coverage are well supported. The duplicate capability test and GET-body helper are low-priority cleanup; do not elevate unreachable middleware or theoretical serialization concerns.

```text
{"consensusReached":false,"consensusSummary":"No practical consensus: reviewers still disagree on a material, authoritative Issue #38 API-contract mismatch. The issue requires `/v1/whoami` to expose `identity`, while the branch exposes `composed_identity`; repository-wide consistency does not resolve that conflict.","reviewerGuidance":[{"reviewer":"OpenCode (Slot 1)","feedback":"Treat the Issue #38 wire contract as authoritative. The `identity` versus `composed_identity` mismatch is blocking; the remaining observations are minor cleanup or speculative hardening."},{"reviewer":"Codex (Slot 2)","feedback":"Your blocking contract finding and incomplete admin-write-route coverage are well supported. The duplicate capability test and GET-body helper are low-priority cleanup; do not elevate unreachable middleware or theoretical serialization concerns."}]}
```


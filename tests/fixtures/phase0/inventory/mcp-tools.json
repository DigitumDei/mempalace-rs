{
  "mempalace_add_drawer": {
    "description": "File verbatim content into the palace. Checks for duplicates first.",
    "input_schema": {
      "properties": {
        "added_by": {
          "description": "Who is filing this (default: mcp)",
          "type": "string"
        },
        "content": {
          "description": "Verbatim content to store \u2014 exact words, never summarized",
          "type": "string"
        },
        "room": {
          "description": "Room (aspect: backend, decisions, meetings...)",
          "type": "string"
        },
        "source_file": {
          "description": "Where this came from (optional)",
          "type": "string"
        },
        "wing": {
          "description": "Wing (project name)",
          "type": "string"
        }
      },
      "required": [
        "wing",
        "room",
        "content"
      ],
      "type": "object"
    }
  },
  "mempalace_check_duplicate": {
    "description": "Check if content already exists in the palace before filing",
    "input_schema": {
      "properties": {
        "content": {
          "description": "Content to check",
          "type": "string"
        },
        "threshold": {
          "description": "Similarity threshold 0-1 (default 0.9)",
          "type": "number"
        }
      },
      "required": [
        "content"
      ],
      "type": "object"
    }
  },
  "mempalace_delete_drawer": {
    "description": "Delete a drawer by ID. Irreversible. Local deletion first by ID; if not found locally, falls back to remotes in name order. Does not use write routing.",
    "input_schema": {
      "properties": {
        "drawer_id": {
          "description": "ID of the drawer to delete",
          "type": "string"
        }
      },
      "required": [
        "drawer_id"
      ],
      "type": "object"
    }
  },
  "mempalace_diary_read": {
    "description": "Read complete diary entries by entry_id for full detail, or list recent entries with filters. Always local; never federated.",
    "input_schema": {
      "properties": {
        "agent_name": {
          "description": "Filter by diary author (optional, default: all agents)",
          "type": "string"
        },
        "entry_id": {
          "description": "Retrieve the complete entry for this wake-up entry identifier",
          "type": "string"
        },
        "last_n": {
          "description": "Number of recent entries to read (default: 10)",
          "type": "integer"
        },
        "since": {
          "description": "Return entries filed at or after this RFC 3339 timestamp (optional, default: 24 hours ago)",
          "type": "string"
        },
        "topic": {
          "description": "Filter by topic tag (optional, default: all topics)",
          "type": "string"
        },
        "wing": {
          "description": "Filter by wing (optional, default: all wings)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "mempalace_diary_write": {
    "description": "Write a diary entry with a concise summary of at most 400 characters. Project-scoped entries are stored in the specified project wing; agent-scoped entries are stored in the shared wing_agents diary. The agent name is recorded as author attribution, not as the storage partition. Always local; never federated.",
    "input_schema": {
      "properties": {
        "agent_name": {
          "description": "Your name \u2014 recorded as the diary author",
          "type": "string"
        },
        "entry": {
          "description": "Your diary entry",
          "type": "string"
        },
        "summary": {
          "description": "Required summary (at most 400 characters) covering the outcome and important TODOs",
          "type": "string"
        },
        "scope": {
          "description": "Where to store the entry: agent or project (optional, default: agent)",
          "enum": [
            "agent",
            "project"
          ],
          "type": "string"
        },
        "topic": {
          "description": "Topic tag (optional, default: general)",
          "type": "string"
        },
        "wing": {
          "description": "Project wing for project-scoped entries. Ignored for agent-scoped entries, which always use wing_agents.",
          "type": "string"
        }
      },
      "required": [
        "agent_name",
        "entry",
        "summary"
      ],
      "type": "object"
    }
  },
  "mempalace_find_tunnels": {
    "description": "Find rooms that bridge two wings \u2014 the hallways connecting different domains. E.g. what topics connect wing_code to wing_team? Local palace only; not federated in v1.",
    "input_schema": {
      "properties": {
        "wing_a": {
          "description": "First wing (optional)",
          "type": "string"
        },
        "wing_b": {
          "description": "Second wing (optional)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "mempalace_get_aaak_spec": {
    "description": "Get the AAAK dialect specification \u2014 the compressed memory format MemPalace uses. Call this if you need to read or write AAAK-compressed memories.",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_get_changes_since": {
    "description": "Get all palace changes since a given timestamp. Call this at session start (or when coordinating with teammates) to catch up on what other agents have written. Returns events in chronological order with operation type, affected entity, actor, and timestamp. When federation is active, remote changes are merged in: each event carries an `origin` field (`\"local\"` or `\"remote:<name>\"`), and a top-level `remotes` object reports per-remote `{ next_cursor, count }` or `{ unreachable: true, error }`. CLOCK-SKEW CAVEAT: timestamps across machines are not directly comparable. Persist the per-origin `next_cursor` values from `remotes` and pass them back via `cursors` rather than reusing a single max-timestamp across origins. `limit` applies per origin (local and each remote each receive `limit` events independently).",
    "input_schema": {
      "properties": {
        "cursors": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Per-remote opaque cursor strings from a previous response's `remotes.<name>.next_cursor`. Pass back to continue pagination for specific remotes without re-fetching already-seen events.",
          "type": "object"
        },
        "limit": {
          "description": "Max events to return per origin (default 50)",
          "type": "integer"
        },
        "since": {
          "description": "ISO 8601 timestamp \u2014 return only events after this point (optional, default: epoch)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "mempalace_get_taxonomy": {
    "description": "Full taxonomy: wing \u2192 room \u2192 drawer count",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_graph_stats": {
    "description": "Palace graph overview: total rooms, tunnel connections, edges between wings. Local palace only; not federated in v1.",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_identity_read": {
    "description": "Read the configured identity.txt used by mempalace_wake_up.",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_identity_update": {
    "description": "Update identity.txt for future wake-ups. Use replace for a full corrected identity or append for a dated note.",
    "input_schema": {
      "properties": {
        "agent_name": {
          "description": "Agent making the update (optional)",
          "type": "string"
        },
        "content": {
          "description": "Identity text or note to write, max 16 KiB per update",
          "maxLength": 16384,
          "type": "string"
        },
        "mode": {
          "description": "replace or append (default replace)",
          "type": "string"
        }
      },
      "required": [
        "content"
      ],
      "type": "object"
    }
  },
  "mempalace_kg_add": {
    "description": "Add a fact to the knowledge graph. Subject \u2192 predicate \u2192 object with optional time window. E.g. ('Max', 'started_school', 'Year 7', valid_from='2026-09-01').",
    "input_schema": {
      "properties": {
        "object": {
          "description": "The entity being connected to",
          "type": "string"
        },
        "predicate": {
          "description": "The relationship type (e.g. 'loves', 'works_on', 'daughter_of')",
          "type": "string"
        },
        "source_closet": {
          "description": "Closet ID where this fact appears (optional)",
          "type": "string"
        },
        "subject": {
          "description": "The entity doing/being something",
          "type": "string"
        },
        "valid_from": {
          "description": "When this became true (YYYY-MM-DD, optional)",
          "type": "string"
        }
      },
      "required": [
        "subject",
        "predicate",
        "object"
      ],
      "type": "object"
    }
  },
  "mempalace_kg_invalidate": {
    "description": "Mark a fact as no longer true. E.g. ankle injury resolved, job ended, moved house.",
    "input_schema": {
      "properties": {
        "ended": {
          "description": "When it stopped being true (YYYY-MM-DD, default: today)",
          "type": "string"
        },
        "object": {
          "description": "Connected entity",
          "type": "string"
        },
        "predicate": {
          "description": "Relationship",
          "type": "string"
        },
        "subject": {
          "description": "Entity",
          "type": "string"
        }
      },
      "required": [
        "subject",
        "predicate",
        "object"
      ],
      "type": "object"
    }
  },
  "mempalace_kg_query": {
    "description": "Query the knowledge graph for an entity's relationships. Returns typed facts with temporal validity. E.g. 'Max' \u2192 child_of Alice, loves chess, does swimming. Filter by date with as_of to see what was true at a point in time.",
    "input_schema": {
      "properties": {
        "as_of": {
          "description": "Date filter \u2014 only facts valid at this date (YYYY-MM-DD, optional)",
          "type": "string"
        },
        "direction": {
          "description": "outgoing (entity\u2192?), incoming (?\u2192entity), or both (default: both)",
          "type": "string"
        },
        "entity": {
          "description": "Entity to query (e.g. 'Max', 'MyProject', 'Alice')",
          "type": "string"
        }
      },
      "required": [
        "entity"
      ],
      "type": "object"
    }
  },
  "mempalace_kg_stats": {
    "description": "Knowledge graph overview: entities, triples, current vs expired facts, relationship types.",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_kg_timeline": {
    "description": "Chronological timeline of facts. Shows the story of an entity (or everything) in order.",
    "input_schema": {
      "properties": {
        "entity": {
          "description": "Entity to get timeline for (optional \u2014 omit for full timeline)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "mempalace_list_rooms": {
    "description": "List rooms within a wing (or all rooms if no wing given)",
    "input_schema": {
      "properties": {
        "wing": {
          "description": "Wing to list rooms for (optional)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "mempalace_list_wings": {
    "description": "List all wings with drawer counts",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_search": {
    "description": "Semantic search. Returns verbatim drawer content with similarity scores. Results from mined files include `stale: true` when the source file changed since mining.",
    "input_schema": {
      "properties": {
        "limit": {
          "description": "Max results (default 5)",
          "type": "integer"
        },
        "query": {
          "description": "What to search for",
          "type": "string"
        },
        "room": {
          "description": "Filter by room (optional)",
          "type": "string"
        },
        "wing": {
          "description": "Filter by wing (optional)",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  "mempalace_status": {
    "description": "Palace overview \u2014 total drawers, wing and room counts",
    "input_schema": {
      "properties": {},
      "type": "object"
    }
  },
  "mempalace_traverse": {
    "description": "Walk the palace graph from a room. Shows connected ideas across wings \u2014 the tunnels. Like following a thread through the palace: start at 'chromadb-setup' in wing_code, discover it connects to wing_myproject (planning) and wing_user (feelings about it). Local palace only; not federated in v1.",
    "input_schema": {
      "properties": {
        "max_hops": {
          "description": "How many connections to follow (default: 2)",
          "type": "integer"
        },
        "start_room": {
          "description": "Room to start from (e.g. 'chromadb-setup', 'riley-school')",
          "type": "string"
        }
      },
      "required": [
        "start_room"
      ],
      "type": "object"
    }
  },
  "mempalace_wake_up": {
    "description": "Wake up into the palace. Returns identity.txt, palace status, recent palace changes, current project history when provided, and recent diary entries across all agents. When federation is active the response also includes `remote_changes`: a per-remote map of change events from the last 24 hours (each event carries `origin: \"remote:<name>\"`), unreachable remotes appear as `{ \"unreachable\": true, \"error\": \"...\" }`, and a `next_cursor` is provided per remote for continuation via mempalace_get_changes_since.",
    "input_schema": {
      "properties": {
        "agent_name": {
          "description": "Current agent name for wake-up context (optional, e.g. claude)",
          "type": "string"
        },
        "diary_limit": {
          "description": "Minimum recent diary entries across all agents (default 10; wake-up also includes every entry since diary_since)",
          "type": "integer"
        },
        "diary_since": {
          "description": "Return wake-up diary entries filed at or after this RFC 3339 timestamp (optional, default: 24 hours ago)",
          "type": "string"
        },
        "latest_limit": {
          "description": "Max recent changes across the whole palace (default 8)",
          "type": "integer"
        },
        "project_limit": {
          "description": "Max recent changes for the current project wing (default 8)",
          "type": "integer"
        },
        "wing": {
          "description": "Current project wing for project-specific history (optional, e.g. wing_myproject)",
          "type": "string"
        }
      },
      "type": "object"
    }
  }
}
